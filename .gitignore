# Build artifacts
bin/
obj/

# IDE
.vs/
.vscode/
.idea/
*.user
*.suo

# .NET
*.swp
*.userprefs
[Dd]ebug/
[Rr]elease/
x64/
x86/
[Bb]uild/
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/
project.lock.json
project.fragment.lock.json
artifacts/

# Local secrets — NEVER commit
appsettings.Development.json
appsettings.Local.json
*.pfx
*.env
.env
secrets.json

# OS
.DS_Store
Thumbs.db

# Local DB backups (manual snapshots; large + machine-specific)
db_backup/

# Dev server stdout capture (from `dotnet run | tee dev-server.log`)
dev-server.log
dev-server.err.log

# Phase 8.4 — export-generated workbooks. The exports/ dir itself is
# checked in via .gitkeep so the runtime path exists on fresh clones,
# but its contents are operator data — never commit.
exports/*
!exports/.gitkeep
