{
  "_comment": "Connection string lives in dotnet user-secrets (per BUILD_PROMPT §2 + §15 #12). Run `dotnet user-secrets list --project src/ReceivingOps.Web` to inspect.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
